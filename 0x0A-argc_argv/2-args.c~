#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: integer
 * @argv: array
 * Description: function that prints arguments
 * Return: (0) Always success
 */


int main(int argc, char *argv[])
{
for (i = 0; i < argc; i++)
{
printf("%s\n", argv[i]);
}
return (0);
}
