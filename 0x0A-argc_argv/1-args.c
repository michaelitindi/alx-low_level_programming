#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: integer
 * @argv: array
 * Description: function that prints number of arguments
 * Return: (0) Always success
 */


int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc);
return (0);
}
