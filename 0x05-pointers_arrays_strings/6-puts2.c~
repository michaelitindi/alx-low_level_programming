#include "main.h"

/**
 * print_rev - Entry point
 * @s: pointer variable
 * Description: function that reverses a string
 * Return: void
 */

void puts2(char *str)
{
while (*str != '\0')
{
_putchar(*str);
str = str + 2;
}
_putchar('\n');
}
