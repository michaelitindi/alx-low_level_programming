#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Description: 'checking the value of the last digit'
 * Return: Always 0 (Success)
 */

int main(void)
{
char a = 'a';
while (a < 'z')
{
putchar(a);
a++;
}
return (0);
} 
